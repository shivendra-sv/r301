# r301.dev — PROGRESS

Cross-session tracker. Every implementation session **reads this first** and updates it before stopping (CLAUDE.md protocol). If you are a fresh session: find "Next session" below, verify the previous row's acceptance commands, then work only that prompt's scope.

**Execution model: SERIAL** (decided 31 Aug 2026). One prompt at a time, in number order, each session verifying its predecessor. No parallel sessions, no worktrees — the verify-chain and this single tracker assume linearity.

## Next session

🎉 **M1 is complete — there is no prompt 21 to run yet.** Prompts 21–28 are M3 stubs that PRD §18 says are detailed *after* pilot learnings, and M2 is the pilot soak itself (no prompts). The build is done; what remains is yours.

Verify the state any time with: from repo root `pnpm test` (**776 green**) and `pnpm typecheck`.

### What stands between here and the Curastax pilot

**1. Deploy to staging.** Push `main`. CI typechecks, tests, applies D1 migrations, deploys, then smokes. A1–A4 are already done (`bbd3ed6`), so the deploy step should work — but see 2, because the smoke step will not.

**2. 🚩 Runbook Phase C — now blocking, not merely pending.** Prompt 20 made smoke hard-fail without `SMOKE_API_KEY`. Until these exist, **every deploy workflow fails at its smoke step** (after deploying):
```bash
cd apps/api
pnpm mint-key --env staging --name ci-smoke      # → gh secret set SMOKE_API_KEY_STAGING
pnpm mint-key --env production --name ci-smoke   # → gh secret set SMOKE_API_KEY_PRODUCTION
pnpm mint-key --env production --name curastax-pilot   # hand over out-of-band, shown once
```

**3. Secrets and rules this repo cannot see** — A5 Sentry DSN ×2, A6 `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID`, A8 notifications. And **A7, the zone rate-limiting rule on the redirect path (D24) — PRD §6 lists it as a P0 pilot blocker** and no test can assert it.

**4. Runbook Phase B then Phase D** — confirm staging answers, then the canary link + two UptimeRobot monitors. Note D21's rule: the canary link stays **untagged** so it never pollutes tag aggregates, and its UA stays countable because it is the drift ruler.

### Four open questions are still unanswered (none blocking)

**Q26 has teeth** — a per-item `internal` failure inside a batch reaches nobody (200 response ⇒ no `onError` ⇒ no Sentry). If KV is flapping during a 100-item Curastax send, you would learn it from Curastax, not from monitoring. Worth deciding before the pilot rather than during it. **Q28** (`500` undeclared in the OpenAPI document), **Q27** (`/v1/tags` grows monotonically), **Q25** (per-item `request_id`) can wait.

**Prompt 14's jq acceptance command is subtly wrong** (`.links|length, .next_cursor` parses as `.links | (length, .next_cursor)` and errors on any correct implementation). The intended check is `'(.links|length), .next_cursor'`. Left as-is in the prompt file; noted so a future session does not read it as a failure.

## Status

| # | Prompt | Status | Date | Notes |
|---|---|---|---|---|
| — | Master session: Phase 0–1 (PRD red-team → **v1.0 signed off**, D1–D25) | done | 2026-08-31 | All 3 open questions resolved; every Phase 1 recommendation accepted |
| — | Master session: Phase 2 (scaffold + CLAUDE.md + 5 docs + CI) | done | 2026-08-31 | D26 (contract elaborations) + D27 (dependency set) recorded |
| — | Master session: Phase 3 (prompts 01–20 + M3 stubs 21–28) | done | 2026-08-31 | Serial, task-sized, dependency-ordered |
| — | Master session: Phase 4 (this tracker) | done | 2026-08-31 | — |
| 01 | workspace-tooling | done | 2026-08-31 | **Resolved versions:** hono 4.13.5 · zod 4.5.4 · @hono/zod-openapi 1.6.1 · @sentry/cloudflare 10.72.0 · typescript **7.0.2** · wrangler 4.127.1 · @cloudflare/workers-types 5.20260831.1 · vitest 4.1.11 · @cloudflare/vitest-pool-workers 0.22.0 · tsx 4.23.13. 5 tests green in the workers pool. Pool 0.22 requires the **Vitest 4 `cloudflareTest` plugin** API (`defineWorkersConfig` is gone) — storage isolation likewise changed mechanism, see deviation 2. Also: `onlyBuiltDependencies` (workerd, esbuild) added to `pnpm-workspace.yaml` — pnpm 10 blocks their postinstall binaries otherwise; tsconfig `include` widened to `test/**/*.ts` so tests are typechecked too. |
| 02 | d1-schema-migration | done | 2026-08-31 | `migrations/0001_init.sql` is **byte-identical to the PRD §9 SQL block** (diffed, not eyeballed) — 5 tables, 4 named indexes, no additions. 13 new tests in `test/schema.test.ts`; 18 total. Confirmed empirically: **D1 enforces foreign keys locally** (Miniflare needs no `PRAGMA foreign_keys` — the orphan-insert and cascade tests were RED before the `REFERENCES` clauses and GREEN after). Note `link_tags ON DELETE CASCADE` is **dormant in v1**: D15 deletes are tombstones, so nothing hard-deletes a `links` row until the prompt-28 purge cron — the cascade is tested but unexercised by product code. Beyond the prompt's 6 behaviors, `api_keys.prefix UNIQUE` (D11) also got a test. Stale comment in `harness.test.ts` ("prompt 02 adds the first migration") corrected. No deviations. |
| 03 | http-foundation | done | 2026-08-31 | Surface split in `src/index.ts` (API hosts → `/v1`, redirect hosts → slugs; **any other host serves both**, which is what makes local dev and the acceptance curl work — design.md §1). New: `src/errors.ts` (code→status table pinned by a test, `ApiError`, `envelope`), `middleware/{request-id,errors,json-body}.ts`, `routes/{api,redirect}.ts`. 26 new tests; 44 total. **Contract detail:** 415 is the one status not derivable from the code table (it carries `invalid_request`), so `ApiError` takes an optional status override — used only there. **Verified, not assumed:** Hono caches the request body *text*, so the JSON guard calling `c.req.json()` does not consume the stream for later routes; and `exports.default.fetch(req)` still injects `env` (`ENVIRONMENT: "local"`) now that the default export is a plain handler object rather than a Hono app — prompt 05's health route depends on that. Layout note: added `routes/api.ts` + `middleware/json-body.ts`, not in design.md §10's list, which explicitly permits adjusting names. No deviations. |
| 04 | telemetry | done | 2026-08-31 | `telemetry/logger.ts` (allowlist line) + `telemetry/sentry.ts` (scrubber, options, `reportError`), wired on **both** surfaces via `middleware/request-log.ts`. 17 new tests; 68 total. **4 files carry `D23 pinned`** (2 tests + 2 sources). **Two SDK findings that mattered:** (a) `@sentry/cloudflare` 10.72's `dataCollection` defaults are *permissive* — `cookies: true`, `httpHeaders: {request:true,response:true}`, `httpBodies: [all]`, `urlQueryParams: true` — i.e. the SDK would collect auth headers and request bodies by default, so every category is now switched off **explicitly** and pinned by a test rather than left to a default an upgrade could flip; (b) Hono's `c.req.routePath` is deprecated and, inside `app.use("*")` middleware, plain `routePath(c)` returns the middleware's own `*` — the template needs `routePath(c, -1)`. Unmatched paths log `route: "/*"`, never the raw path (pinned). Sentry reports only unexpected errors — an `ApiError` is a contracted outcome, not an incident. No DSN ⇒ `sentryOptions()` returns `undefined` ⇒ SDK never initialises; verified the Worker still boots and serves under `wrangler dev`. |
| 05 | health-and-smoke-v1 | done | 2026-08-31 | **M0 complete.** `routes/health.ts` (unauthenticated, vars-only — it deliberately touches neither D1 nor KV so a storage outage cannot take the probe down), `scripts/smoke-checks.ts` (pure, importable by the Worker test pool) + `scripts/smoke.ts` (Node entry, tsx). 15 new tests; 83 total. `sentryOptions()` now sets `release` from `GIT_SHA`. Both deploy workflows pass `--var GIT_SHA:${{ github.sha }}`; **verified by `wrangler deploy --dry-run` that `--var` *merges* with the config vars** — `ENVIRONMENT ("staging")` survives alongside `GIT_SHA`, so health keeps reporting its env. Smoke failure paths verified for real: server-down, wrong base and missing `SMOKE_API_BASE` each exit 1 with a named reason. |
| 06 | auth-middleware | done | 2026-08-31 | `services/keys.ts` (format, uniform base62 by **rejection sampling** — `% 62` would bias the first 8 alphabet chars), `db/{queries,types}.ts`, `middleware/auth.ts`. 35 new tests; 118 total. All six §7.6 rejection paths return the **same** 401 message — distinguishing unknown-prefix from wrong-secret from revoked would be an oracle. Malformed shapes are rejected before any D1 call (pinned by a spy), so a junk key costs zero queries. **Ordering decision: auth runs before `jsonBody`** so an unauthenticated caller never gets their body parsed — pinned by a test that returns 401 where the old order returned 415. **Bug caught by TDD:** a failing `last_used_at` write used to 500 an otherwise-valid request; it is now genuinely best-effort in both the `waitUntil` and the no-execution-context paths. Constant-time compare asserted by spy (verified the assertion fails if swapped for `===`), never by timing. **Existing tests updated, not weakened:** every prompt 03/04 test touching `/v1` now authenticates via `test/helpers/auth.ts`. |
| 07 | key-scripts | done | 2026-08-31 | 🚩 **Runbook Phase C unblocked.** `scripts/key-admin.ts` (pure: arg parsing, SQL, output — importable by the Worker test pool) + `scripts/{mint-key,revoke-key}.ts` (Node entries). 40 new tests; 158 total. `@types/node` installed per **ADR D28**, confined to a new `tsconfig.scripts.json` so `src/**` and `test/**` still compile against workerd globals only; `typecheck` now runs both configs. **Two findings from probing rather than assuming:** (a) `wrangler d1 execute --json` returns a `meta` carrying only `duration` against a **local** DB — there is no `changes` field — so rows-affected comes from `UPDATE … RETURNING prefix`, which works identically local and remote; (b) confining `"types"` does **not** stop `src/**` importing `node:*` (see question 12). Labels are restricted to `[A-Za-z0-9_-]` *and* SQL-quoted — the CLI takes a command string with no bind parameters, so escaping alone is not something to rest on. wrangler is spawned with an args array and no shell. The secret prints only **after** the row is stored, so an operator never holds a key that authenticates nothing. Verified end to end against local D1: stored `key_hash` is 64 chars, `prefix` 20, raw key absent; re-revoke and bad args each exit 1. |
| 08 | validation-schemas | done | 2026-08-31 | `src/reserved-slugs.ts` (**290 words**, lowercase/deduped/sorted, versioned), `src/services/destination.ts` (the §7.1 battery, pure), `src/schemas/{fields,create-link,patch-link,list-query}.ts`. 189 new tests; 347 total. **The api-contract needs two error codes out of one parse** (400 `invalid_request` vs 422 `slug_reserved`/`destination_invalid`, prompt 10 behavior 5), which Zod has no slot for — so those issues carry the code in `params` and **`apiCodeOf(issue)` reads it back**; the destination battery and the reserved check run *inside* the schemas, so a route cannot forget them (see question 13). **Verified, not assumed:** the WHATWG parser normalizes `http://2130706433/`, `0x7f.0.0.1` and `017700000001` to `127.0.0.1`, rewrites `[::ffff:127.0.0.1]` to `[::ffff:7f00:1]`, and IDNA-maps circled-letter homoglyphs to `localhost` — so the range checks run on the **parsed** host and see through the v4-mapped v6 form; all four are pinned. `172.15`/`172.32`/`11.x`/`192.169` are asserted *accepted* so the private ranges cannot quietly widen. **Zod findings:** (a) a `.refine` on a strict object still runs after unknown keys are stripped, so `PATCH {"slug":"x"}` reported both "unknown key" and "empty body" — the empty-body check is now `.check()` guarded on `ctx.issues`, so only the accurate one fires; (b) **`listLinksQuerySchema` cannot render its *output* side to JSON Schema** (`active`/`limit` are transforms) — harmless because query params are documented input-side, but pinned in `openapi-compat.test.ts` so **prompt 19 meets it as a red test, not a build-time throw**. That guard was proven to have teeth by flipping it to the output side and watching it fail. `.strict()` kept over `z.strictObject` — the prompt's wording, and Zod marks it a style hint, not deprecated. |
| 09 | slug-service | done | 2026-08-31 | `src/services/slugs.ts` (`generateSlug(rng)` + `resolveSlug({db,custom?,rng?})`) and `findLinkIdBySlug` in `db/queries.ts`. 29 new tests; 376 total. RNG is injected as `(bytes: Uint8Array) => void`; the test's scripted RNG **throws when its stream runs out**, so an implementation that over-draws fails loudly instead of silently padding with zeros. **D16's absurd-odds branch is reproducible, not hypothetical:** `youtube` is a real 7-char entry on the reserved list, so the test injects bytes that spell it and asserts the regeneration. Rejection sampling is pinned from both sides — 247 accepted (`zzzzzzz`), 248–255 each discarded whole-draw — and the collision cap is pinned by **counting RNG draws** (exactly 3, so it stops at the cap rather than drawing a fourth time), not just by the error. `deleted_at` is deliberately **absent** from the existence query: `UNIQUE(slug)` spans tombstones (D15), so a tombstoned slug is `slug_taken` on the custom path and a redraw on the auto path — both tested. Case-sensitivity pinned too: `Launch` resolves free while `launch` is taken. Typed error is `invalid_request`, not the prompt's shorthand `invalid`, so the union is assignable to `ErrorCode` and prompt 10 maps it with `ERROR_STATUS[code]`. **The existence check races the insert by design** — see the hand-off note above. Questions 16–17 raised (retry-budget semantics; the sampler now duplicated between `keys.ts` and `slugs.ts`). |
| — | Maintenance: open-question sweep (Q1–17 + deviation 3) | done | 2026-08-31 | **All 14 open items resolved with Shivendra.** Eleven were confirmations of existing behaviour (no code change): Q1 7-day Time Travel, Q6, Q7, Q10, Q11, Q13, Q14, Q15, Q16, plus deviation 3. Three produced work: **Q17** extracted `src/services/random.ts` (one base62 rejection sampler, was duplicated in `keys.ts` and `slugs.ts`); **Q12** added the `check:no-node-imports` grep guard to `typecheck`; **deviation 3** amended `docs/design.md` §9. Two were written into future prompts so the decisions survive: **Q5** → `prompts/19-openapi.md` behavior 7 (405 sweep), **Q8** → `prompts/13-click-counting.md` behavior 6 annotation (`ua` wiring). 19 new tests; **395 total**. |
| 10 | create-link | done | 2026-08-31 | **First real endpoint.** New: `src/routes/links.ts`, `insertLink`/`upsertTag`/`attachTag` in `db/queries.ts`, `apiErrorFromZod` in `schemas/fields.ts`. Built **on top of pre-existing uncommitted work** for `src/kv/redirects-cache.ts`, `src/serializers/link.ts` and the `LinkRow` type, which arrived from outside the prompt-09 session — Shivendra's call to keep it rather than redo it. 27 new tests; **442 total**. **Structural change: `createApiApp()` now returns `OpenAPIHono<AppEnv>`, not `Hono<AppEnv>`** — required to declare routes with `createRoute`/`app.openapi` so the spec accrues (D22). Its `defaultHook` throws `apiErrorFromZod(result.error)`, which is where prompt 08's tagged issues finally pay off: **422 `slug_reserved` and 400 `invalid_request` come out of one parse**, and `field` names the offender (including the unrecognized key itself, which Zod does not put in `path`). Every prior route test stayed green through the swap. **Prompt 09's hand-off honoured:** `resolveSlug` never reserves, so the links INSERT catches `UNIQUE constraint failed: links.slug` and maps it to **409 `slug_taken`** — and deliberately does *not* report it to Sentry, since losing that race is a contracted outcome, not an incident. **Two SQL details that mattered:** `insertLink` uses `RETURNING *` so the column defaults (`is_active`, `click_count`) come back without a second read; `upsertTag` uses `ON CONFLICT DO UPDATE SET name = excluded.name`, not `DO NOTHING`, because only an *updated* row is RETURNING-visible — `DO NOTHING` yields no row for an existing tag and would need a second query. **Verified against a real Worker, not just the test pool:** the acceptance curl returns the 201 Link shape; all six error paths return the contracted status/code/`field` (422 destination_invalid, 422 slug_reserved, 400 invalid_request ×2, 409 slug_taken, 401, 405); `wrangler kv key get` reads back exactly `{"d":"https://a.com","t":301,"x":null,"a":1}`; and D1 shows the attribution key plus the implicitly created tag. Note `destination` is stored and echoed **verbatim**, not WHATWG-normalized — `https://example.com` comes back without the parser's trailing slash, so a redirect sends exactly what the caller wrote. |
| 11 | idempotency | done | 2026-08-31 | `src/services/idempotency.ts` (the design §5 machine), `src/services/digest.ts` (`sha256Hex`), `src/middleware/idempotency.ts`, wired in `routes/api.ts`. 23 new tests; **465 total**. **Middleware placement is the load-bearing decision:** after auth (the key scopes the reservation), after `jsonBody` (a request that cannot be served should not reserve), but **before schema validation** — so a body that fails the schema reserves and then releases, which is the same net effect as any other failed execution and keeps "only successes finalize" true. **design §5's ordering is normative and non-obvious:** the 24 h window is tested *before* the request hash, so an expired row with a *different* payload is replaced rather than reported as a mismatch — the key is genuinely free again. That has its own test, because the natural implementation checks the hash first and would 409. **Concurrency detail:** `replaceStale` deletes by `(key, api_key_id, created_at)`, a compare-and-delete, so a reservation another isolate created between our read and our delete is never clobbered; losing the subsequent re-insert race is reported as in-flight, which is what it is. **Replay is proven not to re-execute** by mutating `response_body` in D1 and asserting the mutated bytes come back verbatim — a test that passes trivially if you only compare slugs. Purge uses a `rowid IN (SELECT … LIMIT ?)` subquery: SQLite supports `DELETE … LIMIT` only when built with that option. Verified empirically that Hono yields `""`, not `undefined`, for an empty `Idempotency-Key` header, so the 1–255 check is what rejects it. **Live acceptance run:** two byte-identical POSTs returned the same slug `ROEYkar` with `Idempotency-Replayed: true` on the second; the same key with a different payload returned 409; a new key created a new link; D1 shows one link per distinct key and `demo-1` finalized at 201. Question 18 raised (`keys.ts` still carries its own sha256 hex). |
| 12 | redirect-path | done | 2026-09-01 | **The product's reason to exist.** New: `src/services/redirect.ts` (pure matrix evaluation), `getRedirect` in `kv/redirects-cache.ts`, `findLinkBySlug` in `db/queries.ts`, and the housekeeping + slug routes in `routes/redirect.ts`. 51 new tests; **516 total**. **The matrix is table-driven and run twice — once seeded into KV, once into D1** — so the equivalence that makes KV a *cache* rather than a second source of truth is asserted, not assumed. `findLinkBySlug` is deliberately unfiltered by `deleted_at`: the caller must tell a tombstone from a live row, because a tombstone is a 404 that must **not** be backfilled. **D17's ordering is pinned:** deactivated-AND-expired is **404, not 410** — deactivation outranks expiry so a takedown always looks never-existed. No-negative-caching is asserted by *absence* (unknown and tombstoned slugs leave the KV key unset), which is the only way to catch a well-meaning "cache the miss" regression. HEAD is built through the same `respond()` helper as GET and then has its body withheld, so status and headers cannot drift apart. **Deviation 4 raised:** this prompt's text still says `/` serves "200 landing text", but ADR **D29** — and both `api-contract.md` and `design.md` §2, which were updated for it — make `/` a **302 → `https://www.r301.dev/`**. I implemented D29. **Unspecified call:** `robots.txt`/`favicon.ico` carry `public, max-age=86400`; nothing in the spec sets a cache header for the housekeeping 200/204, and caching static housekeeping for a day is the conventional choice. **Verified live, not just in the pool:** 302 + verbatim `Location` (signed query intact) + `no-store` + `X-Request-Id`; `?utm_source=…` dropped; `/` → marketing site; 204 favicon; 405 on POST; 404 for `/slug/`, `/a/b`, `/ab`; HEAD returning 0 bytes; and the whole D20 loop — delete the KV key, serve 302 off D1, watch it backfill, confirm an unknown slug leaves no key. |
| — | Maintenance: question 18 + deviation 4 | done | 2026-09-01 | **All open items cleared.** Q18 — `keys.ts:hashKey` folded into `digest.ts:sha256Hex` (one delegate line; 81 prompt-06/07 tests green through it). Deviation 4 — D29 confirmed; `prompts/12-redirect-path.md` corrected in three places with a dated note, **PRD §7.5 deliberately left as signed off** since D29 is its amendment record. Prompt 12's unspecified housekeeping cache header is now **specified**: `api-contract.md`'s redirect-host table gives `robots.txt` and `favicon.ico` `public, max-age=86400`, so it is contract rather than an incidental code choice. |
| 13 | click-counting | done | 2026-09-01 | `src/bot-denylist.ts` (design §7's starter set verbatim, 23 lowercase substrings, **UptimeRobot deliberately absent** — it is D21's drift ruler) and `src/services/counting.ts` (`shouldCount` + `recordClick`), wired into `serveSlug` through `ctx.waitUntil`. 29 new tests; 545 total. **`serveSlug` is now built by `createServeSlug(report)`** — the counter's failure has to reach the *injected* reporter, which a module-level handler could not see. The `waitUntil`-or-await shim prompt 12 grew inside `deferBackfill` is extracted as `defer()` and shared by both deferrals; as before, the await degrades to awaiting the work only when there is no execution context, which is tests. **`LogFields.ua` is wired (question 8):** `requestLog` became `createRequestLog({ userAgent })`, opt-in so widening telemetry is always a call site someone wrote — the redirect app passes `true`, the API app keeps the bare `requestLog` export and its narrower allowlist. **Missing UA counts** (absence is not bot proof; biasing the metric the flattering way is the failure mode that matters) — noted in the `shouldCount` doc comment per the prompt. **One prompt-12 test changed, not weakened — see question 19.** Dev-server check ran for real: two browser-UA GETs → `click_count` 2, then a default-curl-UA GET, a browser HEAD and an unknown slug all left it at 2; live log lines carry `ua` with route template `/:slug`, no destination, no slug. |
| 14 | read-and-list | done | 2026-09-01 | The read side. New: `src/services/cursor.ts` (opaque base64url keyset codec), `listLinks` + `findLiveLinkBySlug` + `findTagNamesForLinks` in `db/queries.ts`, and the two GET routes in `routes/links.ts`. 56 new tests; **601 total**. **Structural change forced by the path family: `registerCreateLinkRoute` is now internal and `registerLinkRoutes(app)` is the export.** `methodNotAllowed` registers `app.all(path)` and Hono matches in registration order, so prompt 10's guard — registered at the end of the create function — would have answered **405 to every GET /v1/links**. Both 405 guards now come after every method handler for their path, in one function, so prompts 15/16 add handlers above the guards rather than rediscovering this. **Keyset, not offset, is asserted rather than assumed:** the interleaving test creates three newer links *between* two page fetches and pins that the earlier cursor still yields the original older page. **The `(created_at, id)` tie-break is falsified, not just tested** — deleting the `id` half of the comparison and the `id DESC` sort was verified to turn both tie tests red, which is the only way to know they cover the skip/duplicate bug. `next_cursor` is `null` exactly at exhaustion because the query fetches `limit + 1` and the extra row's presence is the signal — a full page is not treated as evidence of more. Tags for a page come back in **one** query (a 100-item page would otherwise cost 100 round trips), ordered by `link_tags.rowid` so they read back in the order they were attached — see question 21. **Two prompt-04 telemetry tests were restated, not weakened (question 20)** — both had scaffolding that only worked while `/v1/links/:slug` was *not* a real route. No deviations. |
| 15 | update-link | done | 2026-09-01 | **Mutation is live.** New: `updateLink` + `detachAllTags` in `db/queries.ts`, `patchLinkRoute` + `replaceTags` in `routes/links.ts`. 36 new tests; **637 total**. Prompt 08's `patchLinkSchema` needed no changes — strict subset, empty-body check and `nullable()` clears all arrived with it, and `slug` is rejected as an unknown field purely by being absent from the schema. **A mutation test found a real bug, not a hypothetical one:** deleting `detachAllTags` (making tags merge instead of replace) originally failed **one** test, because the handler echoed `body.tags` in the response instead of reading back what was stored. `link_tags` is keyed `(link_id, tag_id)`, so `{"tags":["b","b"]}` stores **one** row while the echo claimed two — the PATCH response disagreed with the next GET. The handler now reads the set back from D1; the same mutation fails **seven** tests. **`registerLinkRoutes` takes an injected `now`** (threaded from `ApiAppOptions`) so the `updated_at` bump is asserted as an exact value rather than a range — see question 24 for the asymmetry this leaves with create. `updateLink`'s `WHERE` carries `AND deleted_at IS NULL`, so a link tombstoned between the route's lookup and its write is not resurrected — `RETURNING` yields nothing and the route renders the same 404. **D20 is pinned hard:** replacing the awaited `putRedirect` with a fire-and-forget put fails **nine** tests, four of which are full round-trips through the real redirect app (deactivate → 404, reactivate → 302, 302→301 flips both status and `Cache-Control`). Acceptance verified against `wrangler dev`, not only in the pool. No deviations. |
| 16 | delete-link | done | 2026-09-01 | **Single-link lifecycle complete.** New: `tombstoneLinkBySlug` in `db/queries.ts` and `deleteLinkRoute` in `routes/links.ts`; `removeRedirect` (written in prompt 12, unused until now) finally has its caller. 25 new tests; **662 total**. **The delete is one statement, not read-then-write:** `UPDATE … WHERE slug = ?1 AND deleted_at IS NULL RETURNING id` gives the contract's semantics for free — unknown and already-tombstoned both match nothing, so both render the *same* 404 with no branch to get wrong, and there is no window between a lookup and the write. **KV gets a `delete`, not a put with `a:0`** — that distinction is the whole point of D15 vs deactivation, and it is pinned: swapping `removeRedirect` for a `putRedirect({a:0})` fails 3 tests, because a tombstoned slug must leave KV **unset** so the redirect falls through to D1 and 404s without backfilling (prompt 12's no-negative-caching rule, re-asserted here through `createExecutionContext`/`waitOnExecutionContext` since the backfill would happen in `waitUntil`). **Three mutations, all caught:** put-instead-of-delete → 3 failures; fire-and-forget the KV delete → 4; dropping `AND deleted_at IS NULL` → 2 (the double-delete 404). **Prompt 15's `updateLink` guard paid off immediately** — PATCH-after-DELETE 404s with no new code, and now has a test saying so. D15's reuse block is asserted end-to-end through the real POST route (409 `slug_taken`), not just against the slug service, and the rejected recreate is checked to leave both the tombstone and the empty KV slot untouched. `link_tags` rows survive (the cascade fires only on a real row deletion, which is prompt 28's purge) and are invisible to every caller. Verified against `wrangler dev` **and** in D1: the row is still there, destination intact, `deleted_at` set, 1 `link_tags` row. No deviations. |
| — | Maintenance: open-question sweep (Q19–24) | done | 2026-09-01 | **All six open questions resolved with Shivendra; the queue is empty for the first time since prompt 13.** Four were confirmations of existing behaviour with no code change — **Q19** (prompt 13's tightened KV-hit test), **Q20** (prompt 14's two restated telemetry tests), **Q21** (tag order = attach order; `docs/api-contract.md` deliberately left unamended, since its Link example already implies the rule), **Q22** (a malformed slug is 404, not 400, on GET/PATCH/DELETE alike). Two produced work, both in prompt 10's create route: **Q23** — create echoed `body.tags` instead of reading the stored set back, so `POST {"tags":["x","x"]}` claimed two tags where `link_tags`' `(link_id, tag_id)` key had stored one, and the create response disagreed with the very next GET; create now reads back exactly as PATCH does. **Q24** — `registerLinkRoutes`'s injected `now` is now shared by create, PATCH and DELETE, so `created_at` is assertable and prompt 17's batch inherits one clock instead of a choice. 3 new tests; **665 total**. Every one of prompt 10's 27 existing tests stayed green through both changes, and both fixes were re-verified against `wrangler dev`. No deviations. |
| 17 | batch-create | done | 2026-09-01 | **Bulk create is live; the create path now has one definition.** New: `services/links.ts` (`createLink` — resolve slug, insert, attach tags, read them back, awaited KV put), `schemas/batch-create.ts`, `routes/batch.ts`. `registerCreateLinkRoute` was reduced to a caller of `createLink`, so single and batch create cannot drift. 30 new tests; **695 total**. **The hand-off's two traps were both real and both are now pinned by tests:** batch registers before the `:slug` family (a test asserts POST is not swallowed into 405), and `/v1/links/batch` was added to the idempotency `app.use` list (a test asserts the replay). Each guard was mutation-checked — removing it fails exactly the test that covers it. **Items are validated per item, not by the wrapper:** the wrapper schema types `links` as `unknown[]` and only enforces the api-contract's whole-request faults (missing / non-array / empty / >100), because a bad item must become one `error` entry rather than a 400 for the other 99 (§7.2). The published document stays honest anyway — `.meta()` renders the real create body, pinned in `openapi-compat.test.ts` for prompt 19. **A non-`ApiError` inside an item (the awaited KV put, D20) becomes `internal` on that item and the loop continues**; its D1 row is left committed exactly as single create leaves it. |
| 18 | stats-and-tags | done | 2026-09-01 | **The numbers are exposed; M1's read surface is complete.** New: `aggregateTagStats` + `listTagCounts` in `db/queries.ts`, `serializers/stats.ts`, `schemas/stats-query.ts`, `routes/stats.ts` (`/v1/links/{slug}/stats` + `/v1/stats`), `routes/tags.ts`. 23 new tests; **718 total**. **Two SQL details carry the whole D15 contract and both are mutation-checked:** `aggregateTagStats` writes `l.deleted_at IS NULL` out explicitly (tombstoning does **not** remove `link_tags` rows, so without it a deleted link keeps contributing), and `listTagCounts` uses **LEFT JOIN** so a tag whose last link was tombstoned still lists at `link_count: 0` rather than vanishing. Six mutations were run against the two queries (drop either tombstone filter, LEFT→inner JOIN, drop `ORDER BY`, drop `COALESCE`, read tombstones in link stats) — each fails exactly one test. `isoOrNull` was exported from `serializers/link.ts` rather than copied, so the ISO/null timestamp rule still has one implementation. **Route order was a non-issue this time**, confirmed rather than assumed: `/v1/links/:slug/stats` is three segments and `app.all("/v1/links/:slug")` is two, so the 405 guard cannot swallow it — a test pins that POST to the stats path is 405, not 404. |
| 19 | openapi | done | 2026-09-01 | **`GET /v1/openapi.json` serves OpenAPI 3.1 — and `docs/api-contract.md` is now commentary; code is canonical.** That file carries a dated banner saying so. New: `routes/openapi.ts`, `schemas/error-envelope.ts` (envelope + the shared 401/405/415 response sets), `schemas/resources.ts` (every response body). 38 new tests; **756 total**. **Served with `doc31`, not `doc`** — `doc` emits 3.0; the API surface was read off the installed package's own `.d.mts` rather than from memory, since the hosted docs only show the 3.0 form. **`info.version` is `GIT_SHA ?? "dev"`**, the same string `/v1/health` reports, so a document and a probe from one deploy cannot disagree about which deploy they are. **Security is a document-level default** (`bearerAuth` on everything) with `security: []` on the two exempt paths — a route added without thinking about auth is documented as authenticated, which is what the middleware actually does to it. **Three lists are now derived rather than hand-maintained**, each pinned by a test that compares the document against the code that enforces the behaviour: `UNAUTHENTICATED_PATHS` (auth middleware) ↔ the operations marked public; `IDEMPOTENT_PATHS` (newly exported from the idempotency middleware, and now what `routes/api.ts` mounts from) ↔ the operations documenting 409; and the Hono router's own `/v1` paths ↔ the document's paths. **Serializer types are now `z.infer` of the published schemas**, so a documented field the code stops sending is a type error rather than a lie in the contract. `registerHealthRoute` moved from `app.get` to `app.openapi` — it was the only route missing from the document. Four mutations were run against the structural guards (drop a 405 guard ×2, un-document a route, drop the security default); each fails exactly the intended test. |
| 20 | full-smoke-and-m1-sweep | done | 2026-09-01 | **M1 is complete.** `scripts/smoke.ts` went from health-only to the full `docs/testing.md` §5 lifecycle: health → create (tagged `smoke`) → fetch → manual-redirect → stats → delete → redirect-404, **7 requests** against a `MAX_SMOKE_REQUESTS` budget of 8 (D25). The sequence, the assertions and the cleanup live in `scripts/smoke-checks.ts` (importable by the Worker pool, no `node:*`); `smoke.ts` only maps env vars in and an exit code out. 20 new tests; **776 total**. **Verified against a real local Worker, not just stubs:** `FULL-SMOKE-OK`, and the smoke link confirmed tombstoned in D1 afterwards. Failure paths driven for real too — missing key prints the Phase C message and exits 1; a wrong key exits 1 naming the step (`POST /v1/links: returned 401`). **The redirect step reads its expected destination from the create response** rather than the literal we sent, because the API normalises a bare origin (`https://example.com` → `https://example.com/`) and asserting the literal would fail on a correct implementation. **One honest negative result:** the cleanup `finally` is *not* distinguishable by test — `step()` already contains every transport and parse failure, so a mutation moving the two cleanup calls into the `try` passes the suite. It stays as defence in depth and the comment says exactly that. The other four mutations (drop `redirect: "manual"`, drop the `smoke` tag, stop requiring the key, break a step) each fail the intended test. |
| 21–28 | M3 hardening stubs | stub | | **Do not run.** Detailed after pilot (M2) learnings; sessions pointed here must stop and ask |

Statuses: `todo` · `in-progress` · `done` · `blocked` · `stub`. A session marks its row `in-progress` on start, `done` (with date + notes) before stopping.

## Deviation log

Anything that diverged from PRD/docs/prompt — **with a question for Shivendra; never improvised silently.** New dependencies also need a `docs/decisions.md` ADR row.

| # | Date | Session/prompt | What diverged | Why | Approved? |
|---|---|---|---|---|---|
| 1 | 2026-08-31 | master | Master-session Phase 2 spec listed `ADMIN_TOKEN` under `wrangler secret put`; the scaffold/runbook omit it | PRD D14 (signed off) removed the admin token and `/v1/keys` from v1 | ✅ yes (Phase 2 gate) |
| 2 | 2026-08-31 | prompt 01 | `docs/testing.md` §2 says `vitest.config.ts` configures "isolated storage per test". The declarative `isolatedStorage` pool option **no longer exists** in `@cloudflare/vitest-pool-workers` 0.22.0 (absent from its options schema and from `dist/`). Isolation is instead implemented in `test/setup.ts` as `afterEach(reset())` from `cloudflare:test`. | Upstream API change (Vitest 4 / pool 0.22). Required behaviour is unchanged and is now pinned by a test — it was RED (KV value leaked into the next test) before the setup file and GREEN after. | ✅ yes (2026-08-31) — `docs/testing.md` §2 amended to describe the setup-file mechanism; the deprecated `SELF`/`env` note in the same section was corrected at the same time. |

| 3 | 2026-08-31 | prompt 04 | `docs/design.md` §9 says `beforeSend` reduces "any URL down to origin+path-template". Applied literally to a **breadcrumb** URL — which is an *outgoing* fetch, i.e. a destination — origin+path still names the destination host (`https://clinic.example.com/appt/9182`). PRD §12 D23 says destination URLs **never** reach Sentry. I implemented the PRD: `event.request.url` is reduced to origin+path (it is our own inbound URL, safe), while breadcrumb `data.url` is **dropped entirely**. | PRD wins over an elaboration doc (CLAUDE.md source-of-truth order). Reducing is demonstrably insufficient for destinations — the RED test failed on exactly this, leaking `clinic.example.com` through a breadcrumb. | ✅ **yes (2026-08-31)** — PRD reading confirmed; `docs/design.md` §9 now carries the rule as its own bullet. Note the doc never actually contained the "reduce any URL" wording this entry quoted — §9 simply did not specify URL handling at all, so the amendment is an addition, not a correction. |

| 4 | 2026-09-01 | prompt 12 | `prompts/12-redirect-path.md` says the objective includes "`/` landing text" and behavior 7 asserts "`/` → 200 text". **ADR D29** (approved 31 Aug 2026) instead makes `/` a `302` → `https://www.r301.dev/` with `no-store`, and both `docs/api-contract.md` (redirect-host table) and `docs/design.md` §2 step 1 were already updated to match. I implemented **D29**. | The prompt file was written in master-session Phase 3, *before* D29 existed; D29's own text says "prompt 12 implements the `/` redirect", so the prompt text — not the decision — is the stale artifact. Docs outrank the prompt in CLAUDE.md's source-of-truth order. | ✅ **yes (2026-09-01)** — D29 confirmed. `prompts/12-redirect-path.md` corrected (objective, behavior 7, and a dated note above §Spec references); **PRD §7.5 left untouched** — it is signed off, and D29 in the ADR log is the post-sign-off amendment mechanism, so editing the PRD would stop the log being the sole record. |

## Open questions for Shivendra

| # | Raised by | Question | Status |
|---|---|---|---|
| 1 | master | After runbook A8: what is the actual D1 Time Travel window on the free tier? Record it here + amend PRD §11 note if ≠ 7 d assumption. | **resolved 2026-08-31 — 7 days; the PRD's assumption holds.** No PRD §11 amendment needed. |
| 2 | prompt 01 | `@types/node` is **not** in D27, so `vitest.config.ts` avoids `node:*` imports (the migrations dir is a cwd-relative literal). Prompt 07's `scripts/*.ts` (mint-key, smoke) run under `tsx` in Node and will need `process`/`node:crypto` types. Add `@types/node` to the approved set then (new ADR row), or keep `scripts/**` out of `tsc`? | **resolved 2026-08-31 — approved: add `@types/node`** (ADR D28). Install it in prompt 07, not before; `vitest.config.ts` stays `node:*`-free either way. |
| 3 | prompt 01 | `typescript@7.0.2` is what `latest` resolves to today — a major-version jump. Everything typechecks green under the existing strict flags, and the lockfile pins it. Confirm TS 7 is intended for this project, or pin back to 6.x? | **resolved 2026-08-31 — keep TS 7.** Routine version currency within D27; no ADR needed. |
| 4 | prompt 01 session (runbook A2) | **Apex conflict — blocks production deploy, not M0/M1.** `r301.dev` already serves a Cloudflare **Pages** static site via a proxied CNAME (verified live: HTTP 200 `text/html`; all four hostnames resolve to Cloudflare anycast IPs). PRD §8 and `wrangler.toml` put the production redirect route on `r301.dev/*`, and Workers routes **take precedence over a Pages custom domain on the same hostname** — so the first `v*` tag would shadow the static site entirely. Staging (`staging.` / `api-staging.`) is dedicated and unaffected, so prompts 02–20 proceed normally. Options: **(a)** move redirects to a dedicated host (e.g. `go.r301.dev`) — amends PRD §8 + the production route, needs an ADR, and costs the short-link brevity the apex buys; **(b)** move the static site to `www.r301.dev` and give the apex to the shortener — no code change, but the apex `/` then behaves per the PRD's housekeeping route, not as a landing page; **(c)** scope the Worker route to a prefix (e.g. `r301.dev/r/*`) — changes short-link shape. Note a same-zone Worker cannot `fetch()` the Pages custom domain, so "proxy through to Pages" is not a clean fourth option. | **resolved 2026-08-31 — option (b).** Static site moved to `www.r301.dev`; apex keeps its PRD §8 role. Recorded as **ADR D29**, which also changes the `/` housekeeping route to a 302 → `https://www.r301.dev/`. `wrangler.toml` unchanged; **prompt 12 implements the `/` redirect.** |
| 5 | prompt 03 | **Hand-off, not blocking.** 405 is *not* automatic: `methodNotAllowed(app, path)` must be called **after** each route's method handlers, per path (Hono has no built-in 405, and a global scan of `app.routes` is unreliable because `app.use()` registrations appear there as method `ALL`). Every route prompt (10, 14–18) must call it, or a wrong method silently 404s instead of 405. Should prompt 19 (OpenAPI) add a test that asserts every registered `/v1` path answers 405 to a bogus method, so this can't be forgotten silently? | **resolved 2026-08-31 — yes, sweep in 19.** Prompt 19 asserts every registered `/v1` path answers 405 (not 404) to a bogus method, driven from the same route list the OpenAPI document is generated from, so a new path cannot escape it. Written into `prompts/19-openapi.md` as behavior 7. |
| 6 | prompt 03 | The redirect surface's **500** currently falls through to Hono's default handler, which happens to return `text/plain` "Internal Server Error" — pinned by a test so a Hono upgrade can't silently turn it into JSON/HTML on the hot path. `api-contract.md` §Redirect host has no 500 row. Want an explicit handler + a `no-store` header there in prompt 12, or is the framework default fine? | **partly resolved by prompt 04** — the redirect surface now has an *explicit* `onError` (plain text, reports to Sentry), because PRD §15 says Sentry covers "API + redirect path" and a silent hot-path failure was a real gap. The **`no-store` header** question is still open for prompt 12. **Fully resolved 2026-08-31 — leave as is.** No `no-store` on the redirect 500; prompt 04's explicit plain-text `onError` stands unchanged. |
| 7 | prompt 04 | **D23 end-to-end coverage is proven as a three-link chain, not one test:** (1) a spy proves the 500 path calls the reporter, (2) a pinned test proves `sentryOptions().beforeSend === scrubEvent`, (3) a pinned test proves the scrubber strips everything. What is *not* asserted is that the SDK actually invokes `beforeSend` before transmitting — that is SDK contract, not our code. Closing that link needs a real client + stub transport, which needs `stackParser`/`integrations` from **`@sentry/core` as a direct import** — a new dependency requiring an ADR (D27). Worth an ADR + a heavier end-to-end test, or is the three-link chain sufficient? | **resolved 2026-08-31 — the three-link chain is sufficient.** No `@sentry/core` direct dependency and no D27 amendment: whether the SDK honours `beforeSend` is its contract, not our code. |
| 8 | prompt 04 | The redirect surface now logs one line per request (PRD §15). The **`ua` field stays unwired** — prompt 04's out-of-scope defers it to 12/13, and `LogFields.ua` exists but nothing sets it. Confirm prompt 12 or 13 owns wiring it, since neither prompt's text currently mentions the log line itself. | **resolved 2026-08-31 — prompt 13 owns it.** The question's premise was wrong, though: prompt 13's behavior 6 *already* required the `ua` log assertion. It is now annotated to say 13 is also where the wiring happens, since it already reads the UA for the D21 denylist and one read should feed both. |
| 9 | prompt 05 | **`tsc` is greener than it looks for `scripts/**`.** `@cloudflare/workers-types` declares `declare const process: any`, so `scripts/smoke.ts` typechecks with *zero* real typing — a misspelled `process.env.SMOKE_API_BAES` would pass silently. I did **not** pre-empt ADR D28 (which says install `@types/node` in prompt 07); instead `smoke.ts` narrows `process` once, at a single commented boundary, which immediately caught a genuine `string \| undefined` bug. Prompt 07 should install `@types/node` **and** decide how: adding it to `tsconfig.json`'s explicit `types` array would also expose Node globals to `src/**`, which D28's own consequences say must stay Node-free — so a separate `tsconfig` for `scripts/**` is probably the right shape. Confirm the approach before prompt 07 implements it. | **resolved 2026-08-31 in prompt 07** — `@types/node` installed, `tsconfig.scripts.json` added, `typecheck` runs both. The Node-free-`src` half of the goal turned out not to work this way; see question 12. |
| 10 | prompt 06 | **Middleware ordering — my call, please confirm.** Nothing in the PRD or `api-contract.md` fixes the precedence between auth and the JSON-only guard. I put **auth first**, so an unauthenticated request with a bad `Content-Type` gets 401 rather than 415, and no unauthenticated body is ever parsed (cheaper under D24's flood posture, and the conventional ordering). The visible consequence: 415/400 are now only reachable by authenticated callers. If you would rather protocol errors surface before auth, say so and I will flip it plus the pinned test. | **resolved 2026-08-31 — keep auth first.** No change. |
| 11 | prompt 06 | `generateKey` accepts `"live" \| "test"` because PRD §7.6's format section names both markers, but **nothing mints test keys in v1** (D13 defers them to P1) and the `environment` column just carries whatever is inserted. So the `test` branch is reachable, tested, and unused. Leave it as format-completeness, or drop the parameter until P1 actually needs it? | **resolved 2026-08-31 — keep for format completeness.** Prompt 27 will need the branch. No change. |
| 12 | prompt 07 | **The `types` split does not fence off `node:*` imports — I checked, and my first assumption was wrong.** `tsconfig.scripts.json` gives `scripts/**` real Node globals (so `process.env` is finally typechecked), but `"types"` controls *global* inclusion, not module resolution: a probe file at `src/_boundary_probe.ts` importing `node:fs` typechecks clean under the main config, because @types/node is resolvable from `node_modules`. So D28's consequence — "nothing in `src/` may import `node:*` without its own ADR" — is documented but **unenforced**. Cheapest real enforcement without a new dependency is a grep guard in the `typecheck` script (`! grep -rE "from \"node:" src/`); a lint rule would need an ADR for the linter. I did not add either — out of this prompt's scope. Want the grep guard? | **resolved 2026-08-31 — grep guard added.** `check:no-node-imports` runs ahead of both `tsc` passes; no new dependency, no ADR. Verified against a probe that it catches all four forms (`from "node:…"`, bare `import`, dynamic `import()`, `require()`), names file and line, and passes on a clean `src/`. |
| 13 | prompt 08 | **Validation is wired *inside* the schemas — my call, please confirm.** Nothing in the PRD or docs fixes where the destination battery and reserved-slug check run. I put them **in the Zod schemas** (`destinationSchema`, `slugSchema`) rather than leaving them as separate service calls for the route, because prompt 10 builds the route from `@hono/zod-openapi` route definitions and would otherwise have to *remember* to call them — the same silent-omission footgun as question 5's 405. The cost: Zod has one error channel but the api-contract needs three codes (400 `invalid_request`, 422 `slug_reserved`, 422 `destination_invalid`), so those issues carry the code in `params` and `apiCodeOf(issue)` reads it back. Prompt 10 maps issue → envelope with that one helper. If you would rather the route call `validateDestination()`/`isReservedSlug()` explicitly and keep the schemas shape-only, say so now — it is a small change here and a large one after 10/15/17 are built on it. | **resolved 2026-08-31 — keep the checks inside the schemas.** No change. |
| 14 | prompt 08 | **`limit` rejects rather than clamps.** Prompt 08 says "`limit` clamps/rejects out of 1–100" — both. I chose **reject** (400 `invalid_request`), because `api-contract.md` lists "bad query param" under `invalid_request`, and a silently clamped `?limit=1000` returns a 100-item page that looks like exhaustion to a client paging by page size — a clamp hides the client's bug, an error names it. Confirm, or switch to clamping? | **resolved 2026-08-31 — reject, do not clamp.** No change. |
| 15 | prompt 08 | **A too-long destination is 422, not 400 — one place the api-contract can be read both ways.** §Field constraints says `destination` is "≤ 2048 chars … Violations → `destination_invalid`" (422), while the error table gives `invalid_request` (400) for a generic "schema violation". I followed the **specific row**: every §7.1 destination rule, length included, returns 422 `destination_invalid`. The alternative reading — length is a schema violation (400), safety is 422 — is defensible and would split one rule set across two status codes. Confirm 422 for all of them, or split? | **resolved 2026-08-31 — 422 `destination_invalid` for every §7.1 destination rule, length included.** No change. |
| 16 | prompt 09 | **Retry budget: 3 attempts total, and a reserved hit spends one.** Two spec texts differ slightly — `docs/design.md` §6 says "Retry ≤3 on UNIQUE violation" (which reads as 1 initial + 3 retries = 4 draws), prompt 09 says "3 consecutive collisions → typed internal error" (3 draws). I implemented **3 total draws**, matching the prompt, and pinned it by counting RNG calls. Second, smaller call: my loop spends one attempt on a *reserved* hit as well as on a collision, rather than giving reserved regeneration its own unbounded budget — an unbounded reserved loop would hang on a pathological RNG, and at 62⁷ against 290 words the two can never both fire. Confirm 3-total, or switch to 4 and/or separate budgets? Either is a one-line change now and a behaviour change after prompt 10 builds on it. | **resolved 2026-08-31 — 3 draws, shared budget.** No change. |
| 17 | prompt 09 | **The base62 rejection sampler now exists twice, near-verbatim.** `services/keys.ts` (`randomBase62`, prompt 06) and `services/slugs.ts` (`generateSlug`, this prompt) carry the same 62-char alphabet literal, the same `248` threshold and the same draw-and-discard loop. They differ only in that slugs take an injected RNG and keys call `crypto.getRandomValues` directly. I did **not** unify them — CLAUDE.md forbids drive-by refactors, and changing `keys.ts` would touch prompt 06's pinned tests. But this is security-relevant code, and a fix applied to one copy would silently miss the other. Proposal: extract `randomBase62(length, rng)` into a shared module (`src/services/random.ts`), have both call it, and keep every existing test green. Worth a small dedicated cleanup — now, or folded into prompt 20's M1 sweep? | **resolved 2026-08-31 — extracted now.** `src/services/random.ts` owns `BASE62` + `randomBase62(length, rng)`; `keys.ts` and `slugs.ts` both call it and the duplicates are gone. 19 new tests pin the sampler contract directly at length 4 — so they pin the sampler, not something incidental to the slug length. **The extraction surfaced a real typing bug:** `random.ts` reaches `scripts/**` through `keys.ts`, where Node's `getRandomValues` rejects `Uint8Array<ArrayBufferLike>`, so `RandomBytes` is now pinned to `Uint8Array<ArrayBuffer>`. Every prompt-06/09 test stayed green throughout. |
| 18 | prompt 11 | **A third sha256-hex copy was avoided, but a second still stands.** Prompt 11 needed `sha256(raw body bytes)` for `request_hash`, so it added `src/services/digest.ts` (`sha256Hex`) rather than growing a private copy. `services/keys.ts` (prompt 06) still has its own byte-identical implementation inside `hashKey`. I did **not** fold it in — that is a drive-by refactor of prompt 06 code, which CLAUDE.md forbids — but it is exactly the shape of question 17, which you chose to extract immediately. Proposal: `hashKey` becomes a one-line delegate to `sha256Hex`; prompt 06's known-vector SHA-256 test already guards the change. Do it as a small cleanup, or leave the two copies? **resolved 2026-09-01 — folded in.** `keys.ts:hashKey` is now a one-line delegate to `digest.ts:sha256Hex`; the duplicate implementation is gone and `digest.ts` is genuinely the one place. Prompt 06's known-vector SHA-256 test and the whole auth/scripts suite (81 tests) stayed green through the change. | resolved |
| 19 | prompt 13 | **I changed a prompt-12 test's assertion — please confirm.** `"serves a KV hit without querying D1"` counted every `db.prepare()` and asserted **0**. Counting makes a KV hit issue exactly one D1 statement (the §7.4 `UPDATE`), so the test failed the moment the counter was wired. What D20 forbids is *resolving a redirect* from D1, not touching D1 at all — so I renamed it `"serves a KV hit without looking the slug up in D1"` and pinned it harder than before: **no `SELECT` reaches D1, and exactly one statement total** (so a future accidental read is still caught, and so is a second write). The alternative — sending a denylisted UA so counting never fires — would have kept the old assertion by testing a case the D20 invariant does not care about. Confirm the tightened test, or prefer it restated some other way? | **resolved 2026-09-01 — confirmed.** The tightened test stands as written: it catches strictly more than the original (a stray `SELECT` *and* a second write), and what D20 forbids is resolving a redirect from D1, not touching D1 at all. |
| 20 | prompt 14 | **I restated two prompt-04 telemetry tests — please confirm.** Both broke the moment `GET /v1/links/:slug` became a real route, and neither break was a regression. (a) `"logs method, status, latency…"` built its app as `createApiApp()` *plus* a stub `app.get("/v1/links/:slug")`; the real route now matches first and 404s (nothing seeded), so the stub is gone and the test seeds a real row — same assertions, one less fixture between them and the code. (b) `"never logs the raw path, even when no route matched"` asserted `route === "/*"` for `/v1/links/SECRET_SLUG`, a path that **now matches** a route; since the test is specifically about the *unmatched* case, it now requests `/v1/nosuchcollection/SECRET_SLUG`. The load-bearing assertion — the raw path never appears in the log line — is untouched in both. Confirm, or prefer either restated differently? | **resolved 2026-09-01 — confirmed.** Both restatements stand. Each now pins its assertion against the real route rather than a stub, and the load-bearing half (the raw path never reaching the log line) is untouched. |
| 21 | prompt 14 | **Tag order inside the Link resource is unspecified — I picked attach order.** `api-contract.md` fixes tag order for `GET /v1/tags` ("sorted by name") but says nothing for the `tags` array on a Link, and the contract's own Link example (`["tenant:42", "kind:appointment"]`) is **not** name-sorted. Prompt 10's create echoes the array the caller supplied, so reads sorting by name would mean the same link comes back in a different tag order than it was created with. I made reads return **attach order** (`ORDER BY link_tags.rowid`), which matches create and the example. The cost: it leans on `rowid` (fine — `link_tags` is an ordinary rowid table) and prompt 15's PATCH must re-attach in the supplied order for the property to hold. Confirm attach order, or switch both create and read to name-sorted? | **resolved 2026-09-01 — attach order, as implemented.** No code change. `docs/api-contract.md` deliberately left unamended: its Link example already implies the rule. Verified live that all four tag-returning paths agree — create, PATCH, GET one and list all read `link_tags.rowid` order, so `["tenant:42","kind:appointment"]` comes back unsorted. Prompt 15's `replaceTags` re-attaches in the caller's order, which is what keeps the property true through an update. |
| 22 | prompt 14 | **A malformed slug on fetch-one is 404, not 400 — confirm before 15/16 copy it.** `api-contract.md` §GET one says only "Unknown or tombstoned → 404", so I put **no format schema** on the `{slug}` path param: `/v1/links/ab` (too short to be a valid slug) answers 404 exactly like `/v1/links/nosuch`. The alternative — validating the param against the slug regex — would answer 400 for `ab` and 404 for `abc`, splitting one outcome ("no such link") across two statuses. Whichever you pick, **PATCH and DELETE in prompts 15–16 must match it**, which is why it is worth settling now rather than three times. | **resolved 2026-09-01 — 404 for anything not found.** No code change; GET, PATCH and DELETE already agree. A malformed slug is simply one that cannot exist, and splitting "no such link" across two statuses buys nothing. |
| 23 | prompt 15 | **`POST /v1/links` echoes duplicate tags; PATCH no longer does — should create be fixed too?** Verified against `wrangler dev`: `POST` with `{"tags":["x","x"]}` responds `["x","x"]`, while an immediate `GET` of the same link responds `["x"]`. `link_tags` is keyed `(link_id, tag_id)` and `attachTag` is `INSERT OR IGNORE`, so one row is stored and the create response is simply wrong about it. Prompt 15 hit the identical bug and fixed it by reading the tag set back from D1 before serializing; prompt 10's create still echoes `body.tags`. The fix is the same one line, and prompt 10's tests stay green through it — but it is prompt 10 code, which CLAUDE.md keeps out of this session's scope. Fold it in now (like questions 17/18), or leave it for prompt 20's M1 sweep? | **resolved 2026-09-01 — folded in.** Create now reads the tag set back from D1, exactly as PATCH does. Verified live: `POST {"tags":["x","x","y"]}` and an immediate GET both report `["x","y"]`; before the fix create claimed `["x","x","y"]`. All 27 pre-existing prompt-10 tests stayed green through the change. |
| 24 | prompt 15 | **Two routes in one file now read the clock differently — my call, please confirm.** Prompt 15's behavior 1 asks for a fake clock on the `updated_at` bump, so `registerLinkRoutes` now takes an injected `now` threaded from `ApiAppOptions` (the same option auth and idempotency already use), and the test asserts `updated_at` is *exactly* the injected instant rather than merely greater than `created_at`. Create still calls `Date.now()` inline for `created_at`, because changing it is prompt 10 code. So the file has one clock-injected route and one that is not. Unify them — create takes the same injected clock — now, or at prompt 20? (Note `expiresAtSchema` also calls `Date.now()` directly inside its refine, so the "must be future" rule is on the real clock either way; that one is prompt 08 code and is arguably correct as-is, since a validation rule about *now* should not be mockable by a route's clock.) | **resolved 2026-09-01 — unified.** `registerLinkRoutes`'s injected `now` is shared by create, PATCH and DELETE, so `created_at` is assertable and prompt 17's batch inherits one clock rather than a choice. `expiresAtSchema` still calls the real `Date.now()` inside its refine, deliberately: a "must be in the future" rule should not be mockable by a route's clock. |
| 25 | prompt 17 | **Per-item `error` objects omit `request_id` — confirming the reading.** The api-contract's §batch example (which prompt 17 calls "normative") shows a per-item error as `{code, message, field}` with no `request_id`, while the prompt's prose says "envelope-shaped error objects" and the global convention says "error bodies echo it as `request_id`". I followed the **normative example**: a batch is a 200 success body, it already carries `X-Request-Id` once on the response, and repeating one id inside up to 100 items is noise rather than supportability. If you want the full envelope per item instead, say so — it is one line in `routes/batch.ts::errorItem` plus a test. | open |
| 26 | prompt 17 | **A per-item `internal` error reaches nobody — batch is a Sentry blind spot.** In single create, a failed awaited KV put becomes a 500, so `app.onError` reports it and §15's "durable forensics = Sentry + D1 state" holds. In batch the same failure becomes one `error` item inside a **200**, so `onError` never runs, `reportError` is never called, and the request log line reads `status: 200` — an infra outage during a 100-item Curastax send would be invisible except to the caller. I did **not** add reporting: prompt 17 scopes the behaviour as "that item is `error internal`, the batch continues" and nothing more, and injecting `reportError` into the batch route is a design choice, not a detail. Options if you want it closed: (a) call the injected `reportError` per unexpected item, (b) log a single allowlisted line carrying the failed-item count, (c) accept the blind spot until P1. | open |
| 27 | prompt 18 | **`/v1/tags` grows monotonically — zero-count rows never leave, and that changes what D26.6's ~1,000 threshold means.** Nothing prunes a `tags` row: tombstoning a link leaves its `link_tags` rows in place, and a PATCH that replaces a tag set detaches without deleting the now-orphaned tag. Both were **observed on the local dev D1**, which still lists `a`, `b`, `c`, `x`, `y`, `z` at `link_count: 0` from prompts 14–17's dev-server checks. Behaviour 6 of prompt 18 asks for exactly this ("assert and document"), so it is implemented and tested, not a deviation — but two consequences are worth your call before prompt 19 publishes the contract: (a) the "revisit past ~1,000 tags" trigger counts **ever-used** tags, not currently-used ones, so it will fire earlier than D26.6 implies; (b) Curastax's tag list will slowly fill with dead `tenant:*` entries once clinics churn. Options: leave it (honest, and a zero-count tag is real history), filter `link_count > 0` from the response, or add a prune to the P1 purge cron alongside the tombstone sweep (prompt 28). | open |
| 28 | prompt 19 | **`500 internal` is the one contract status the OpenAPI document does not declare.** `docs/api-contract.md` defines it ("Unexpected. Also returned when the awaited KV write fails — retry with the same `Idempotency-Key`, it converges, D20"), and it is a real, reachable outcome on create, PATCH and DELETE. I documented the other two silent statuses this prompt — **405** (universal; every path registers a guard, and the sweep test proves it) and **415** (deterministic, already tested in `http.test.ts`) — because both are exact and mechanical. I stopped at 500 because *where* to declare it is an editorial choice about the published contract, not a fact I can read off the code: (a) on every operation, which is honest but adds a line of noise to all eleven; (b) only on the three write paths the contract explicitly names, which under-describes the read paths that can also fail unexpectedly; or (c) not at all, treating 500 as implicit for any HTTP API. My preference is (b) — it matches what the contract actually promises — but it is your call, and prompt 20's M1 sweep is the natural place to land it. | open |
## M1 exit sweep (prompt 20, 1 Sep 2026)

Every **P0** item from PRD §6 and the §8 endpoint summary, mapped to what covers it. Built by reading the tree, not from memory.

| P0 item (PRD §6 / §8) | Where it lives | Proven by |
|---|---|---|
| Shorten endpoint (`POST /v1/links`) | `routes/links.ts` → `services/links.ts` | `routes/create-link.test.ts` (30) |
| Redirect edge (`GET /{slug}`) | `routes/redirect.ts` | `routes/redirect-path.test.ts` (21) |
| Custom slugs | `services/slugs.ts`, `schemas/fields.ts` | `services/slugs.test.ts` (23), `reserved-slugs.test.ts` (8) |
| Expiry (lazy, 410) | `schemas/fields.ts`, `services/redirect.ts` | `redirect-path.test.ts`, `schemas/create-link.test.ts` |
| Deactivate (`is_active`) | `routes/links.ts` PATCH | `routes/update-link.test.ts` (36) |
| Click counting + UA denylist (D21) | `services/counting.ts`, `bot-denylist.ts` | `services/counting.test.ts` (10), `routes/click-counting.test.ts` (9) |
| API key auth (D10–D12) | `middleware/auth.ts`, `services/keys.ts` | `middleware/auth.test.ts` (18), `services/keys.test.ts` (8) |
| Bulk create (≤100) | `routes/batch.ts` | `routes/batch-create.test.ts` (25) |
| Tags (implicit create, filter, list) | `db/queries.ts`, `routes/tags.ts` | `routes/tags.test.ts` (6), `list-links.test.ts`, `stats.test.ts` |
| Delete — tombstoned (D15) | `routes/links.ts`, `tombstoneLinkBySlug` | `routes/delete-link.test.ts` (25) |
| Update destination | `routes/links.ts` PATCH | `routes/update-link.test.ts` (36) |
| Idempotency, D1-backed (D18) | `middleware/idempotency.ts`, `services/idempotency.ts` | `routes/idempotency.test.ts` (23) + batch replay |
| `external_id` passthrough (D19) | `schemas/*`, `db/queries.ts` | `schemas/create-link.test.ts`, `list-links.test.ts` |
| Key mint/revoke scripts (D14) | `scripts/mint-key.ts`, `revoke-key.ts` | `scripts/key-admin.test.ts` (21) + integration (5) |
| `GET /v1/health` (D25) | `routes/health.ts` | `routes/health.test.ts` (7) |
| No-sensitive-telemetry (D23) | `telemetry/sentry.ts`, `logger.ts` | **pinned**: `sentry-scrubber.pinned.test.ts` (13), `logger-allowlist.pinned.test.ts` (3) |
| Sentry error reporting | `telemetry/sentry.ts` | `telemetry/error-reporting.test.ts` (5) |
| D1 migrations | `migrations/0001_init.sql` | `schema.test.ts` (13), re-applied from zero per test |
| `GET /v1/links` list/filter | `routes/links.ts` | `routes/list-links.test.ts` (36) |
| `GET /v1/links/{slug}` | `routes/links.ts` | `routes/read-link.test.ts` (10) |
| `GET /v1/links/{slug}/stats`, `GET /v1/stats`, `GET /v1/tags` | `routes/stats.ts`, `routes/tags.ts` | `routes/stats.test.ts` (16), `tags.test.ts` (6) |
| `GET /v1/openapi.json` (D22) | `routes/openapi.ts` | `routes/openapi.test.ts` (22) |
| Staging + production environments | `wrangler.toml` — real D1/KV IDs pasted | runbook A3/A4, commit `bbd3ed6` |
| Post-deploy smoke (PRD §14) | `scripts/smoke-checks.ts` | `scripts/smoke-checks.test.ts` (25), plus a real local run |

**Two P0 items are not code and are not done.** Neither is a gap in the build; both are Shivendra's hands:
- **Zone rate-limiting rule on the redirect path (D24, runbook A7)** — a Cloudflare dashboard rule. PRD §6 lists it as a **pilot blocker**, and nothing in the repo can assert it.
- **Sentry DSN secrets (A5) / GitHub Actions secrets (A6) / notifications (A8)** — set outside the repo, so this sweep cannot confirm them either way. They are listed as unverified rather than done.

**Workflow env-var names checked against the script, name by name:** `deploy-staging.yml` and `deploy-production.yml` both pass `SMOKE_API_BASE`, `SMOKE_REDIRECT_BASE` and `SMOKE_API_KEY` — exactly the three `readSmokeConfig` requires. No mismatch.

⚠️ **CI consequence of this prompt, stated plainly:** smoke now **hard-fails without `SMOKE_API_KEY`**. Until runbook **Phase C** mints the two `ci-smoke` keys and stores them as `SMOKE_API_KEY_STAGING` / `SMOKE_API_KEY_PRODUCTION`, every deploy workflow will fail at its smoke step — the deploy itself will already have happened. That is the intended design (a deploy nobody smoke-tested should not report success), but it means **Phase C is now blocking, not merely pending**.

## Milestone checklist (mirrors PRD §18)

- [x] **M0 — Foundation** = prompts 01–05 — **complete 31 Aug 2026.** Runbook Phase A+B can now be exercised end-to-end: a push to `main` typechecks, tests, applies migrations, deploys staging and smokes `GET /v1/health`. Phase A2–A6 must be done first or those runs stay red.
- [x] **M1 — Core API** = prompts 06–20 — **complete 1 Sep 2026.** Every P0 endpoint, the redirect path, KV write-through, counting + UA denylist, idempotency, key scripts, the OpenAPI document and the full post-deploy smoke. 776 tests green; `tsc` clean. See the M1 exit sweep above for the item-by-item mapping.
- [ ] **M2 — Pilot** — no prompts: Curastax integration (their side), runbook Phases B–D, 4-week soak, §17 exit criteria
- [ ] **M3 — Hardening** = prompts 21–28, detailed after pilot learnings
- [ ] **M4 — Public launch**
- [ ] **M5 — UI** (post-launch)

## Runbook gates (docs/runbook.md — Shivendra's hands only)

| When | Items |
|---|---|
| **Before prompt 01** | A1 only (Node + corepack; `wrangler login` can wait until A3) |
| **Before first push to GitHub / first deploy** | **A1–A4 done** (commit `bbd3ed6`, 31 Aug 2026): zone + DNS `100::` (apex/Pages conflict resolved by D29 — static site on `www`), and real D1 + KV ids are pasted into `wrangler.toml` for both envs. **Unverified from the repo — they live in Cloudflare/GitHub/Sentry:** A5 Sentry DSN secrets ×2 · A6 `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID` · A7 zone rate rule (**a PRD §6 P0 pilot blocker**) · A8 notifications + Time Travel check. |
| **After prompt 07** | Phase C: mint `ci-smoke` keys ×2 → `SMOKE_API_KEY_STAGING`/`_PRODUCTION` GH secrets; mint `curastax-pilot` key |
| **Before pilot (M2)** | Phase B checks · Phase D: canary link + UptimeRobot ×2 + alert channel |
