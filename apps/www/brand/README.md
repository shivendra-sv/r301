> **Source only — never uploaded.** This directory sits one level above `../public/`, which is what Cloudflare Pages ships. The web assets listed under `root` below (favicon.ico, favicon.svg, safari-pinned-tab.svg, apple-touch-icon, android-chrome 192/512, maskable 512, site.webmanifest, og-image) live in `../public/`; `snippet.html` was folded into `../public/index.html` (6 Sep 2026).

# r301.dev logo kit

Direction 3c: "r301.dev" in Archivo 800 (".dev" regular, neutral) with two rules beneath — full-width ink (the long URL) and short red (the short one).

Colors: ink #201e1d · ground #f3f2f2 · red #ec3013. Zero radius. Font: Archivo (Google Fonts).
SVG lockups/avatars reference Archivo by name — convert text to outlines before using where the font isn't available. PNGs are already rasterized.

svg/   lockup light/dark + mono fallbacks · mark light/dark/on-ink · avatar ink/red
png/   lockup 1x/2x/4x light+dark · mark 16–512 (transparent + on-ink) · avatar 512/1024
root   favicon.ico (16/32/48), favicon.svg, safari-pinned-tab.svg, apple-touch-icon (180), android-chrome 192/512, maskable 512, site.webmanifest, og-image (1200×630), snippet.html (head tags)

Clear space ≥ 2× red-rule height around the lockup. Minimum lockup width 96px; below that use the mark.
