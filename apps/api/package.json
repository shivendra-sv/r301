{
  "name": "@r301/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:no-node-imports": "if grep -rEn '(from|import|require)[[:space:]]*\\(?[[:space:]]*\"node:' src/; then echo \"D28: src/ must not import node:* (PROGRESS question 12)\"; exit 1; fi",
    "typecheck": "pnpm run check:no-node-imports && tsc --noEmit && tsc --noEmit -p tsconfig.scripts.json",
    "openapi": "tsx scripts/dump-openapi.ts",
    "openapi:check": "tsx scripts/dump-openapi.ts --check",
    "smoke": "tsx scripts/smoke.ts",
    "mint-key": "tsx scripts/mint-key.ts",
    "revoke-key": "tsx scripts/revoke-key.ts"
  },
  "dependencies": {
    "@hono/zod-openapi": "^1.6.1",
    "@sentry/cloudflare": "^10.72.0",
    "hono": "^4.13.5",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260831.1",
    "@types/node": "^26.4.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.1"
  }
}
