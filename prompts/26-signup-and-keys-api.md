# Prompt 26 — Signup + /v1/keys self-service (M3 STUB)

**STATUS: STUB — do not implement from this file.** Detailed after pilot learnings (M2 soak), per the master plan.

Scope when detailed: minimal `users` table (additive — D3), email-based signup issuing one key, self-service `POST/GET/DELETE /v1/keys`, visibility becomes per-user (D12's planned evolution), cross-user objects present as 404 (D22). Refs: PRD §7.6 Bootstrap, D14, §6 P1.

If you are an implementation session pointed here: stop and ask Shivendra.
