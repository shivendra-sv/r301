# Prompt 21 — Rate limiting + quotas (M3 STUB)

**STATUS: STUB — do not implement from this file.** Detailed after pilot learnings (M2 soak), per the master plan.

Scope when detailed: per-key Workers Rate Limiting bindings (writes/reads split), per-key storage quotas, `rate_limited` 429 + headers. Refs: PRD §12 P1, D6, D24, §6 P1.

If you are an implementation session pointed here: stop and ask Shivendra.
