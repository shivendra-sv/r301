{
  "name": "r301",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "openapi": "pnpm --filter @r301/api openapi",
    "openapi:check": "pnpm --filter @r301/api openapi:check"
  }
}
